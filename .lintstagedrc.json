{
    "*.css": [
      "prettier --write"
    ],
    "*.{js,vue}": [
      "prettier --write",
      "eslint --ignore-path .gitignore --fix",
      "vitest related --run --environment=jsdom"
    ]
  }
